{
  "name": "sf-validation-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "heroku-postbuild": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": "18.x"
  }
}
